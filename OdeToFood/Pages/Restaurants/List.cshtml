
@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
    ViewData["Title"] = "test";
}

<h1>Restaurants</h1>
<form method="get">
    <div class="form-group">
        <div class="input-group">
            <div class="input-group">
                <input type="text" class="form-control" asp-for="SearshTerm">
                <div class="input-group-append">
                    <button type="submit" class="btn btn-secondary">Search</button>
                </div>
            </div>
        </div>
    </div>
</form>

@foreach (var restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="restaurant"/>
}

@*<table class="table table-hover">
    @foreach (var restaurant in Model.Restaurants)
    {
        <tr>
            <td>@restaurant.Name</td>
            <td>@restaurant.Location</td>
            <td>@restaurant.Cuisine</td>
            <td>
                <a class="btn btn-lg " asp-page="./Detail" asp-route-restaurantId="@restaurant.ID">
                    <i class="fa fa-search fa-1x"></i>
                </a>
                <a class="btn btn-lg " asp-page="./Edit" asp-route-restaurantId="@restaurant.ID">
                    <i class="fa fa-edit fa-1x"></i>
                </a>
                <a class="btn btn-lg " asp-page="./Delete" asp-route-restaurantId="@restaurant.ID">
                    <i class="fa fa-remove fa-1x"></i>
                </a>
            </td>
        </tr>

    }
</table>*@



<a asp-page="./Edit" class="btn btn-info">Add Restaurant</a>
 
@section footer{ 
<div>@Model.Message</div>
}